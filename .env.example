# Copia este archivo a .env y rellena el valor real.
# .env NO se sube a git (está en .gitignore).
#
# Clave de la API de Anthropic (Claude), usada para mapear las cifras
# extraídas de los PDF de Enagás al catálogo de métricas. Se obtiene en
# https://console.anthropic.com/settings/keys.
ANTHROPIC_API_KEY=
